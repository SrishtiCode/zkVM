[package]
name = "toystark"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Phase 1: a minimal end-to-end STARK for a trivial statement (no CPU yet). Wires interpolate -> LDE -> constraints -> FRI -> verify into one working pipeline."

[dependencies]
field = { path = "../field" }
poly = { path = "../poly" }
