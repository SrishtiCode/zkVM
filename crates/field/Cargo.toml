[package]
name = "field"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Finite field arithmetic for the zkvm STARK pipeline: a small human-graspable toy field and a real 64-bit Goldilocks field."

[dependencies]