[package]
name = "air"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Phase 3: the AIR (Algebraic Intermediate Representation) for the isa crate's CPU — turns an execution trace into polynomial constraints."

[dependencies]
field = { path = "../field" }
isa = { path = "../isa" }
