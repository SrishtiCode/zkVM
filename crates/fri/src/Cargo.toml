[package]
name = "fri"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Phase 5: the FRI low-degree test as its own crate, promoted out of toystark's proof-of-concept and built on the real merkle commitments from Phase 4. Deliberately transcript-agnostic — see ChallengeSource — since Fiat-Shamir itself is Phase 6's job."

[dependencies]
field = { path = "../field" }
poly = { path = "../poly" }
merkle = { path = "../merkle" }
