[package]
name = "poly"
version = "0.1.0"
edition.workspace = true
license.workspace = true
description = "Polynomial arithmetic over a Field: FFT/IFFT, interpolation, and low-degree extension."

[dependencies]
field = { path = "../field" }
